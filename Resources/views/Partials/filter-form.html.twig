{% form_theme form 'bootstrap_3_Layout.html.twig' %}
{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
  <button class="btn btn-secondary">{{ 'Clear'|trans }}</button>
  <button type="submit" class="btn btn-primary">{{ 'Filter'|trans }}</button>
</div>
{{ form_end(form) }}
